# Misc / Comment

`Misc / Comment` preserves free-form preset annotations without modifying the framebuffer. The micro
preset parser accepts either inline text or an explicit `comment=` assignment and forwards the
resulting string to `avs::effects::misc::Comment`.
