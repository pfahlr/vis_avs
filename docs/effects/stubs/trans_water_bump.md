# Trans / Water Bump

The legacy "Trans / Water Bump" effect is now implemented in
`src/effects/trans/effect_water_bump.cpp`. The implementation combines a
height-field water simulation with framebuffer sampling to emulate the
classic bump-mapped ripple distortion.
