# Render / Simple

The legacy "Render / Simple" effect is implemented by
[`src/effects/render/effect_simple_spectrum.cpp`](../../src/effects/render/effect_simple_spectrum.cpp).
It draws analyzer bars or scope traces using the classic AVS spectrum band logic and color cycling.
